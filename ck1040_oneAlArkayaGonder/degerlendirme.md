# Değerlendirme — ck1040 Öne al / arkaya gönder

1. `ustteBtnUst` değişkeni metodun İÇİNDE değil, sınıfın içinde ama metodun DIŞINDA
   tanımlı. Metodun içinde tanımlansaydı ne olurdu?
   > Cevap notu: Her tıklamada sıfırdan `true` olarak yeniden oluşurdu, "hangi durumdayız"
   > bilgisi hiç hatırlanmazdı — buton hep aynı yöne giderdi. Bu ck1250'de derinleşecek.

2. `SendToBack()` ile `BringToFront()` özellik mi metot mu, nereden anladın?
   > Cevap notu: Metot — ikisi de parantezli, bir eylem yaptırıyorlar, değer atamıyorlar.
