# Değerlendirme — ck1194 Ödeme hesaplayıcı (ünite 06 ek kapanışı)

1. `switch (cmbUrun.SelectedIndex)` ile ck1170'teki `if/else if (cmbIslem.SelectedIndex
   == ...)` aynı işi mi görüyor?
   > Cevap notu: Evet — switch, birden fazla sabit değeri karşılaştıran uzun if/else if
   > zincirinin daha düzenli yazılmış hâli, ck0585'teki gibi.

2. `tutar * 1.03` satırı hangi Console dersindeki fikirle aynı?
   > Cevap notu: ck0300'deki KDV/yüzde formülüyle aynı — bir değeri yüzde oranında
   > artırmak için (1 + oran/100) ile çarpma.
